# Infrastructure adapters

This folder will host concrete adapters that implement application ports,
for example:
- JSON/file-based project repository
- UDP show transport
- Audio IO adapter

During migration, adapters may internally delegate to existing managers.
